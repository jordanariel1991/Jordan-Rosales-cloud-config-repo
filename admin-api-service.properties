# this is the port on which our admin-api-service will run
server.port=8282

# allow for RefreshScope
management.endpoints.web.exposure.include=*

# the admin-api-service will use this as the greeting that it serves up
officialGreeting="Hello, this is the admin-api-service"
