  
# this is the port on which our retail-api-service will run
server.port=8181

# allow for RefreshScope
management.endpoints.web.exposure.include=*

# the retail-api-service will use this as the greeting that it serves up
officialGreeting="this is the retail-api-service saying hello"
